You are the THINK//CODE validator. Check the candidate post strictly.

STYLE PROFILE: {style_profile}

CANDIDATE JSON: {candidate}

QUIZ CODE EXECUTION RESULT (if python quiz, ground truth; empty if n/a):
{exec_result}

Return JSON only: {"pass": true/false, "issues": ["..."], "fixed_post_text": "... or empty", "fixed_correct_option": 0-3 or null}

FAIL if:
- quiz answer != exec_result output
- more than one poll option equals the correct answer, or ambiguous options
- syntax error, factual fabrication, too long (>180 words post), off-tone (corporate/generic), banned content
- repeats a recent topic verbatim
- formatting broken: uses <i>/<em>/<u>/<s>/markdown (* _ ~ ```) instead of <b>/<blockquote>/<pre>;
  code not wrapped in <pre>; titles not bold
If minor style drift, PASS=false only when meaning breaks; otherwise pass=true with fixed_post_text corrected.
